STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
# When set, Redis backs the cache, event bus and workflow engine. Leave unset
# locally to use the in-memory defaults; set it in production (e.g. Upstash).
REDIS_URL=redis://localhost:6379
# Generate strong values with: openssl rand -base64 32
JWT_SECRET=__CHANGE_ME__
COOKIE_SECRET=__CHANGE_ME__
DATABASE_URL=
DB_NAME=medusa-backend
# Set DATABASE_SSL=true when the Postgres provider requires TLS (managed PG).
# DATABASE_SSL=true

# --- File storage (production) ---
# When AWS_ENDPOINT_URL_S3 is set, uploads go to S3-compatible object storage
# instead of the ephemeral local disk. These are auto-set by `fly storage create`
# (Tigris); leave them unset locally to keep the on-disk file provider.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=auto
# AWS_ENDPOINT_URL_S3=https://fly.storage.tigris.dev
# BUCKET_NAME=
# Public base URL for stored files, e.g. https://<bucket>.fly.storage.tigris.dev
# S3_FILE_URL=
